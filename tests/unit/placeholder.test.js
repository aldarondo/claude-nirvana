// All features require unit + integration tests before a task is marked complete.

// Example unit test stub — replace with real tests as features are built.
describe('claude-nirvana unit tests', () => {
  test.todo('heater status parser returns correct on/off state');
  test.todo('temperature reading is within valid range');
  test.todo('schedule validation rejects invalid time formats');
});
