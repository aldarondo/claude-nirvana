// All features require unit + integration tests before a task is marked complete.

// Example integration test stub — replace with real tests as features are built.
describe('claude-nirvana integration tests', () => {
  test.todo('MCP tool call returns heater status from hardware interface');
  test.todo('turn_on tool call changes heater state and confirms via status query');
});
